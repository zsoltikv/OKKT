<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OKKT25.PastTripsPage"
             Title="Korábbi kirándulások"
             BackgroundColor="#F5F5F5">

    <VerticalStackLayout Padding="20" Spacing="15">

        <Label Text="Mentett kirándulások" 
               FontSize="24" 
               FontAttributes="Bold"
               TextColor="#424242"
               Margin="0,10,0,20"/>

        <CollectionView x:Name="TripsCollectionView"
                        SelectionMode="Single"
                        SelectionChanged="OnTripSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame CornerRadius="10" 
                           HasShadow="True"
                           BackgroundColor="White"
                           Padding="15"
                           Margin="0,0,0,10">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={x:Reference Name=TripsCollectionView}, Path=BindingContext.TripTappedCommand}"
                              CommandParameter="{Binding}" />
                        </Frame.GestureRecognizers>

                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                            <Label Text="{Binding TripName}" 
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#1976D2"
                                   Grid.Row="0" Grid.Column="0"/>

                                                <Label Text="{Binding LastSaved, StringFormat='{0:yyyy.MM.dd HH:mm}'}" 
                                   FontSize="12"
                                   TextColor="#757575"
                                   Grid.Row="1" Grid.Column="0"/>

                                                <Label Text="{Binding Participants, StringFormat='{0} fő'}" 
                                   FontSize="14"
                                   TextColor="#424242"
                                   Grid.Row="2" Grid.Column="0"/>

                                            <Label Text=">" 
                                   FontSize="30"
                                   TextColor="#BDBDBD"
                                   VerticalOptions="Center"
                                   Grid.Row="0" Grid.Column="1" Grid.RowSpan="3"/>          
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Label x:Name="EmptyStateLabel"
               Text="Még nincsenek mentett kirándulások"
               FontSize="16"
               TextColor="#9E9E9E"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               Margin="0,50,0,0"
               IsVisible="False"/>

    </VerticalStackLayout>

</ContentPage>